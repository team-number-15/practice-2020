- name: install required soft
  become: true
  yum:
    name: "{{ item }}"
    state: present
    update_cache: yes
    enablerepo: epel
  loop:
    - wget
    - "{{java.VERSION}}"
    - python3 
    - ansible
    - git
